{
  "name": "@lattice-php/map",
  "version": "0.56.3",
  "private": true,
  "description": "Dev workspace for the map component package; ships to consumers via Composer, not npm.",
  "type": "module",
  "scripts": {
    "build:standalone": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lattice-php/core": "0.56.3",
    "@lattice-php/lattice": "0.56.3",
    "@lattice-php/ui": "0.56.3",
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.20"
  }
}
