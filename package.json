{
  "name": "@lattice-php/map",
  "version": "0.72.1",
  "private": true,
  "description": "Dev workspace for the map component package; ships to consumers via Composer, not npm.",
  "type": "module",
  "scripts": {
    "build:standalone": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lattice-php/core": "0.72.1",
    "@lattice-php/lattice": "0.72.1",
    "@lattice-php/ui": "0.72.1",
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "@types/google.maps": "^3.58.1",
    "@types/leaflet": "^1.9.20"
  }
}
